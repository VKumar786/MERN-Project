/* 
    TODO: SSH
    ? https://www.atlassian.com/git/tutorials/git-ssh
    ? ls ~/.ssh
    ? ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
    ? 2 enter for avoiding password
    ? SHA256:c6wlWJjmrZIRM24osRJFwDZDo5DAHiAWh3NdGtl7MCc yadav11adu@gmail.com
    ? eval $(ssh-agent -s)
    ? ssh-add -K ~/.ssh/id_rsa
    ? cat ~/.ssh/id_rsa.pub
    ? ssh -T git@github.com
*/
console.log('vishal')
/* 
    TODO: Update Operation
    ? db.collection_name.updateOne(<filter>, <update>)
    ? operation
        * show dbs
        * use Vishal_inovation
        * show collections 
        * db.vishal_data.updateOne({ name : 'yash'}, {$set : {isMarried : true}})
        * db.vishal_data.find()
        * db.vishal_data.updateMany({ age : 20 }, {$set : {isMarried : true}})
        * db.vishal_data.find()
*/